
# buggy_program.py — Contains 4 bugs. Find and fix them all.

def calculate_stats(numbers):
    total = sum(numbers)
    count = len(numbers)
    average = total / count
    
    above_average = []
    for num in numbers:
        if num > average:
            above_average.append(num)
    
    return {
        "total": total,
        "average": average,
        "above_average": above_average,
        "above_count": len(above_average)
    }

scores = [85, 92, 78, 95, 88, 70, 93]
result = calculate_stats(scores)

print(f"Total: {result['total']}")
print(f"Average: {result['average']}")
print(f"Above average: {result['above_count']} scores")

1. For each bug:
2. Identify the bug
3. Name the error type (SyntaxError, TypeError, KeyError, etc.)
4. Write the fix
Note whether you found it yourself or with AI help


1. “70”
2. TypeError 
3. 70
4. Found this myself

1. print(f"Average: {result[average]}")
2. NameError
3. print(f"Average: {'result[average]'}")
4. Myself

1. def calculate_stats(numbers) has no colon at the end
2.SyntaxError 
3. def calculate_stats(numbers):
4. With AI help

1. if num > average has no colon
2. SyntaxError
3. if num > average:
4. With AI help

Take one of your practice exercise solutions from an earlier lesson (tip calculator, grade analyzer, or inventory manager). Ask an AI tool:

"How can I make this code more Pythonic?"
"What edge cases am I not handling?"
"Are there any security or reliability concerns?"
For each suggestion the AI gives:

Evaluate it using the checklist from this lesson
Decide whether to implement it (and explain why or why not)
If you implement it, verify the code still works

I would implement both the suggestion by AI. The first suggestion is that it is compressing the four identical print lines. It stored the rates in a list and created a loop. This resulted in a much cleaner input. Alongside that it suggested four edge cases to be handled. They are non-numeric billing such as twenty or $45. Now forces the the user to put response for the dollar amount to above zero. And lastly takes care of inputs with decimals.

After the new set of code had been written the code base is still working properly. 